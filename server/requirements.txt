asgiref==3.12.1
Django==6.0.7
django-cors-headers==4.9.0
djangorestframework==3.17.1
dnspython==2.8.0
gunicorn==26.0.0
packaging==26.2
PyJWT==2.13.0
pymongo==4.17.0
python-decouple==3.8
sqlparse==0.5.5
tzdata==2026.3
whitenoise==6.12.0
requests>=2.31.0

# Milestone 2/3 ML pricing model. Required to deserialise and run
# ml/models/freight_pricing_model.joblib; the pricing agent degrades to the
# rule price if they are absent, but the AI price needs them.
joblib>=1.3.0
pandas>=2.0.0
scikit-learn>=1.4.0

pytest>=8.0.0
pytest-django>=4.8.0
